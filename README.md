# FileTransferAndModificationJavaProject
Project aimed at re-learning file transmission in java. 

Goal Create a microservice that reads a file in a batch manner
parses the file and sends it to the kafka stream for another microservice to process the data asynchronously 